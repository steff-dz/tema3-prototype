const nav = document.createElement('nav');
const siteTitle = document.createElement('h1');
nav.id = 'nav';
siteTitle.id = 'siteTitle';
document.body.appendChild(nav);
nav.appendChild(siteTitle);
siteTitle.innerHTML = `SARAH BOWES DESIGN.`;
